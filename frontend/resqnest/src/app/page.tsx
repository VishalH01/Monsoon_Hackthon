// Root route "/" — renders the Landing component from the common Pages folder.
// This file MUST exist for "/" to work: in Next.js a URL only resolves when its
// folder contains a `page.tsx`. The Pages/ folder alone does not create routes.
export { default } from "@/app/Pages/landing";
