// Route for /login — renders the Login component from the common Pages folder.
// Add new pages the same way: put the component in src/app/Pages/<name>.tsx,
// then create src/app/<route>/page.tsx that re-exports it like this.
export { default } from "@/app/Pages/login";
